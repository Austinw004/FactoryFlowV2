# FactoryFlowV2 — Palantir digest — 2026-05-20

> First real run of the Palantir meta-routine. Ontology skeleton created.
> Run executed in REPORT-ONLY / RESTRAINT mode: the dominant finding is a
> runaway routine fleet (50 open PRs, 152 open issues, near-zero merge
> rate). Adding more duplicate PRs/issues would worsen the problem, so this
> run deliberately produced **one** artifact: this digest + the ontology
> snapshot. No new issues filed (all would-be issues already exist).

## 🎯 Top 3 things Austin should know today

1. **The routine fleet is in a feedback loop, not making progress.** 50 open PRs + 152 open issues, almost all routine-generated, near-zero merges. Issue [#253] already names it: *"operator merge required to break the loop."* Until you merge/close in bulk, every routine keeps regenerating the same artifacts.
2. **CI on main is red** — 21 TypeScript errors on real main (`f824ce0`): `server/lib/erp/*` adapters (15), `authPaymentRoutes.ts` (4), `Onboarding.tsx` (2). A fix is already open in PR #263 ("TypeScript 21→0"). Build passes (esbuild doesn't typecheck), which masks the broken gate. This also makes CI on **this** docs PR (#267) red, since the gate runs the full typecheck.
3. **An open P0 outage** ([#175]): `prescient-labs.com` reportedly unreachable (Replit deployment down). Could not verify live from this routine — needs your eyes.

## 👥 Customer signal
- **New leads:** 0 reconciled. The `contact_inquiries` table is not readable from this execution context (no DB access, no dump in repo). `leads.json` seeded empty; reconcile once a read path exists. (`/internal/leads` is correctly gated by `requirePlatformAdmin`.)
- **Top customer ask surfacing from the hats:** none ingested this run (no hat reports in `docs/ontology/inbox/`). The Intel-hat issue backlog (#243/#250/#229/etc.) is heavily skewed to **Section 232 tariff overlay** and **long-lead-time component risk (DRAM/HBM 40–58wk)** — recurring product themes worth a roadmap decision.
- **Pilots at risk:** none known (no customer/pilot records yet).

## 🚨 Incidents & security
- **Open P0/P1:**
  - P0 — prescient-labs.com outage (`INC-2026-05-20-002`; issue #175).
  - P1 — CI red on main, 21 TS errors (`INC-2026-05-20-001`; fix open in PR #263).
  - P1 — routine-fleet runaway / duplicate PRs (`INC-2026-05-20-003`; issue #253).
  - P1 — `.replit` secret rotation pending pre-pilot (`INC-2026-05-20-004`; `SECURITY_ROTATION.md`).
- **Secrets-in-repo scan:** **PASS** — `.replit` has no plaintext `(KEY|SECRET|TOKEN)="..."` matches.
- **Sacred-file attempts blocked:** 0 (no sub-routines executed this run).

## 🛠 Code health
- **Typecheck:** RED — 21 errors on real main (`f824ce0`): erp adapters (15), payments (4), onboarding (2). **Build:** GREEN (esbuild/vite don't typecheck, so build passing masks the broken type gate). **CI on main:** RED. Fix already open in PR #263.
- **Coverage delta vs 7d ago:** n/a (no test script defined; no prior baseline).
- **Dependency advisories:** not measured (npm audit not run); a react-group bump is pending in PR #259.
- **Correction note:** an earlier reading in this run reported 319 errors / "unrelated git histories." That was an artifact of a **stale local `origin/main` ref** (`c356664`). After `git fetch`, real main = `f824ce0` (this branch is a clean +1-commit descendant) with 21 errors. No history anomaly.

## 🗺 Roadmap alignment
- `roadmap.json` is an empty skeleton (now/next/later all empty), so no formal alignment yet. Recurring **product asks** from the open-issue backlog that have no roadmap entry: Section 232 tariff overlay on commodity forecasts (#177/#195/#204/#211/#223/#229…), long-lead-time component risk surface (#243/#251), supplier country/region field for chokepoint exposure (#224). These are candidates for the `next` bucket — proposed, not applied.
- Items on roadmap with no customer ask in 30d: n/a (roadmap empty).

## 🔁 Routines run today
| name | class | status | PR link | note |
| --- | --- | --- | --- | --- |
| palantir-meta (this run) | orchestrator | PARTIAL | (this PR) | Ran Phases 1–2 + 7–8 in restraint mode; skipped Phase 4 sub-routine execution (see Needs Austin). |
| daily-codebase-engineer | B/C | NOT RUN | — | Existing TS-fix PRs already open (#260/#239/#192/#263); deferred to avoid duplication. |
| three-hat | A/B | NOT RUN | — | No fresh inbox reports; existing intel issues already filed. |
| weekly-deep-clean | C | NOT RUN | — | Not Sunday. |

## 🛟 Needs Austin
1. **Break the loop (highest leverage):** triage the 50 open PRs / 152 open issues. Concretely — pick ONE canonical ontology-bootstrap PR (#258, #249, or #174) and close the other two; pick ONE TS-fix PR (the #161 coordinated zod/drizzle upgrade is the right one) and close the competing partial fixes; then merge to get main green. Per [#253], nothing improves until an operator does this.
2. **Confirm the P0 outage** ([#175]) — is `prescient-labs.com` actually down right now?
3. **Main is red (21 TS errors); merge a fix.** PR #263 ("TypeScript 21→0") already targets exactly these errors. CI on this docs PR (#267) is consequently red too — that is the pre-existing gate failure, not anything this PR changed. Once #263 (or equivalent) lands, #267's CI should pass on rebase.
4. **Email connector:** the spec says email the digest to info@prescient-labs.com / austinwendler44@gmail.com. No send-capable email tool is available to this routine, so the digest is persisted to the repo only. Wire an Email connector if you want the daily email.

## 🧬 Routine self-improvement suggestions (drafts — not applied)
- **Meta-routine — Phase 1 dedup gate:** before opening ANY PR/issue, query open PRs+issues and skip if an equivalent already exists. The fleet has re-created the ontology bootstrap ≥3× (#258/#249/#174) and the "main is red" issue ≥2× (#191/#232). Add an explicit "defer to existing artifact, do not regenerate" instruction.
- **Meta-routine — Phase 7 rule 25:** "open a P0 issue `[palantir] main is red`" should first check for an existing equivalent issue and reference it instead of filing a duplicate. (Honored this run: #191/#232 referenced, no new issue filed.)
- **All code-modifying routines:** the build-green-but-typecheck-red split means `npm run build` is a false "all clear." Routines should gate on `npm run check`, not `npm run build`.
- **Branch strategy:** routines keep branching from divergent bases. Standardize on branching off the actual `main` HEAD so PRs show clean diffs.

## 📎 Appendix
- Real `main` SHA at start = at end: `f824ce0`. This branch (`claude/relaxed-ramanujan-9awCo`) = main + 1 docs commit (`fc2e0e7`, later `<updated>`).
- Note: a stale local `origin/main` ref initially read `c356664`; corrected to `f824ce0` after `git fetch`.
- Ontology files created: customers, leads, incidents, code_health, forecasts, roadmap, routines (+ this digest, + meta_runs.jsonl).
- Wall clock: ~partial run. Sub-routine execution (Phase 4) intentionally skipped.
- Open PRs: 50. Open issues: 152.
