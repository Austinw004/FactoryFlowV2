# FactoryFlowV2 — Palantir digest — 2026-05-21

> **Run 1 (bootstrap + restraint mode).** First execution: ontology skeleton created and seeded from real GitHub + code-health signals. **No code-modifying sub-routines were executed this run** — there is already a ~30-PR unmerged backlog and an open duplication report (#253). Piling on more PRs would worsen the exact problem the meta-routine exists to prevent (P3/P6). The highest-value action today is surfacing that backlog to Austin, not adding to it.

## 🎯 Top 3 things Austin should know today
1. **~30 routine PRs are open and unmerged** (#228–#271) and #253 flags duplicate three-hat PRs — the routines are running but nothing is merging. This is the #1 bottleneck. → `incidents.json:OPS-PR-BACKLOG`
2. **Ontology is now live** at `docs/ontology/*` (this PR). World model seeded; from tomorrow every routine has a shared substrate.
3. **Code health is green on this branch** — typecheck clean, build clean — but issue #232 claims CI typecheck is red on `main`. Local check passed, so #232 is likely stale or branch-specific; **needs verification on main HEAD.**

## 👥 Customer signal
- New leads: **unknown** — this run has **no database access** (no secrets, no `contact_inquiries` read path). `leads.json` is an empty skeleton awaiting a dump in `docs/ontology/inbox/` or a read-only query path. (link to /internal/leads for live data)
- Top customer ask surfacing from the hats: **none captured** (no hat reports in `docs/ontology/inbox/`; hat findings live in their own unmerged PR branches).
- Pilots at risk: **Texas pilot** seeded as prospective (`customers.json`); health unknown — no touch data available.

## 🚨 Incidents & security
- Open P0: **none.** Open P1: **2** — #202 `/api/suppliers` >10s timeout in prod; OPS-PR-BACKLOG (process).
- Secrets-in-repo scan: **PASS** — `.replit` has no plaintext `(KEY|SECRET|TOKEN)="..."` matches.
- Sacred-file attempts blocked: **0** (no sub-routines executed).
- npm audit: **8 advisories — 1 high, 7 moderate, 0 critical.**

## 🛠 Code health
- Typecheck: **clean** / Build: **clean** / CI on main: **disputed** (#232 says red; local says green — verify).
- Coverage delta vs 7d ago: **n/a** (no test script in package.json; first run, no baseline).
- Dependency advisories: **8** (`npm audit`). Deps: 96 prod + 23 dev.

## 🗺 Roadmap alignment
- `roadmap.json` was seeded **draft-only** from open product issues; **none are customer-confirmed** (no CRM access). The dominant inferred theme is **Section 232 tariff overlay** (6 issues: #204/#211/#223/#229/#238/#250) — strong internal signal, customer demand unverified.
- Items customers are asking for that aren't on roadmap: **unknown** (no lead/customer data this run).
- Items on roadmap that no customer has asked for in 30d: **unknown** (same reason).

## 🔁 Routines run today
| name | class | status | PR link | note |
|------|-------|--------|---------|------|
| Palantir meta-routine | A/B | ✅ ran | this PR | ontology bootstrap + digest |
| Daily Codebase Engineer | B/C | ⏸ not run | — | restraint: backlog (#233/#239/#255/#260/#263/#268 already open) |
| Three-Hat Intel Analyst | C | ⏸ not run | — | restraint: backlog (#234/#240/#247/#256/#261/#264/#269) |
| Three-Hat Customer/Eng | B | ⏸ not run | — | restraint: backlog (#228/#237/#242/#254/#266) |
| Weekly Deep Clean | C | ⏸ n/a | — | Thursday — Sunday-only routine |
| Dependabot | B | ⏸ external | #259 | react group bump open |

## 🛟 Needs Austin
1. **Triage/merge the open routine PR backlog (~30 PRs).** Until these merge, the routines keep regenerating overlapping work (#253). Recommend a merge-or-close sweep, oldest-first.
2. **Decide the data path for `leads.json`/`customers.json`.** The meta-routine cannot read `contact_inquiries` (no DB access by design). Either drop a periodic dump into `docs/ontology/inbox/` or approve a read-only query path.
3. **Verify CI typecheck on `main` HEAD** (#232). Local build/typecheck on this branch are green; confirm whether #232 is resolved and closeable.
4. **Branch divergence:** `claude/relaxed-ramanujan-IoW26` is **68 behind / 50 ahead** of `origin/main`. Confirm this is the intended base for the ontology, or advise rebasing onto current main before merge.
5. **Email not sent automatically:** no email-send connector is available to this run. This digest is saved to the repo; a Gmail *draft* can be created on request.

## 🧬 Routine self-improvement suggestions
- **Three-Hat routines (vigilant-allen / hopeful-rubin):** add an explicit pre-flight "check for an existing open PR from this routine in the last 48h; if found, append to it instead of opening a new one." Directly addresses #253 duplication.
- **All code-modifying routines:** gate on "is there an unmerged PR from my own slug? If yes, STOP and report instead of stacking." Prevents backlog growth.
- (No `meta_runs.jsonl` history yet — failure-pattern analysis begins next run.)

## 📎 Appendix
- Main SHA at start: `c356664`  ·  Branch SHA: `cd823504`  ·  Main SHA at end: `c356664` (untouched)
- Wall clock: ~0:10  ·  Cost (tokens): approx (single-run, no sub-routine fan-out)
- Files created: `docs/ontology/{routines,leads,customers,incidents,code_health,roadmap,forecasts}.json`, `meta_runs.jsonl`, `digests/2026-05-21.md`
- Verify gates run on this branch: `npm run check` ✅ · `npm run build` ✅ · secrets scan ✅ · sacred files untouched ✅
