enterprise reliability engineer. I need an end-to-end “Texas Pilot Readiness Verification” run that confirms the platform is operating correctly for a mid-market manufacturing pilot (read-only + draft-only; zero production mutations). Do the following in one pass: (1) run the full enterprise certification harness and output the gate-by-gate PASS/FAIL summary plus the total test count; (2) verify that all copilot actions remain draft-only and can never reach completed without approval, including a negative test attempting to bypass approval and proving validateNeverCompleted raises a SAFETY_VIOLATION; (3) verify multi-tenant isolation by attempting cross-tenant reads/updates/deletes for at least SKUs, materials, suppliers, RFQs, allocations, automation rules, and purchase orders and proving all return empty/undefined with no data leakage; (4) verify atomic spend limits with a concurrency test (e.g., 50 concurrent requests) and show allowed/blocked counts and final spend exactly equals the configured limit; (5) verify distributed locks by running at least two simulated workers for each of the background jobs and proving only one executes while others skip, and that stale locks recover after TTL; (6) verify idempotency by forcing the same trigger to fire multiple times and proving createActionIdempotent deduplicates with deterministic trigger event IDs even when JSON key order changes; (7) verify WebSocket tenant isolation by proving broadcastUpdate requires companyId and that messages cannot be sent to all clients; (8) verify pilot evaluation mode guarantees zero production mutations by scanning for any write paths that can execute outside of draft/approval and producing explicit evidence that production-mutation endpoints are disabled or gated. Output: a Markdown report named TEXAS_PILOT_READINESS_REPORT.md plus a JSON artifact with rawEvidence, and include exact commands I can re-run. If anything fails, stop and propose the smallest code change to fix it, then re-run the relevant tests to prove the fix.