Build and run a FINAL “Enterprise Readiness E2E Certification” pass for this codebase. Treat this as the last test before publishing: no half measures, no vague “looks good,” and no fabricated results. I need you to (1) identify any remaining correctness, integrity, security, multi-tenant isolation, automation safety, payment reliability, integration reliability, and data honesty gaps, (2) implement fixes where possible, and (3) produce reproducible evidence scripts and a certification report that I can re-run any time.

Start by creating a new folder server/tests/enterprise-e2e/ and implement a runnable certification harness (TypeScript/Node) that executes an end-to-end suite against the running app (or directly against modules where needed). The harness must output a deterministic, timestamped report to ENTERPRISE_E2E_CERTIFICATION.md and a machine-readable JSON artifact to server/tests/enterprise-e2e/artifacts/certification.json. Every test must clearly show: what it validated, what endpoints/files it touched, what pass criteria are, and the observed evidence (counts, IDs, status codes, and query results). If any test fails, the harness must exit non-zero and print an actionable remediation note.

The suite must cover these gates, with “hard evidence” requirements:

Gate 1 — Multi-tenant isolation is provably enforced everywhere it matters. Enumerate and scan all server routes for “by-id” access patterns. Verify that every GET/PATCH/DELETE by ID for company-scoped entities is enforced by WHERE-clause tenant scoping (companyId in the query), not fetch-then-check. Include negative tests that attempt cross-tenant reads/updates/deletes for at least: automation rules, purchase orders, suppliers, materials, RFQs, allocations, price alerts, and any platform analytics endpoints. Produce evidence showing “Company A cannot access Company B data.” If any unsafe route exists, fix it by moving tenant scoping into the storage layer with company-scoped WHERE clauses and update handlers accordingly.

Gate 2 — Automation spend limits and guardrails are atomic and cannot be bypassed under concurrency. Prove that spend reservation uses a single atomic SQL statement (no TOCTOU) and cannot overshoot under 50+ concurrent requests. Run a concurrency test (50+ parallel executions) and show allowed vs blocked counts and final spend totals. Verify guardrail violation counters use atomic increments, safe mode is checked transactionally immediately before any side-effect, and that approvals cannot be bypassed. If any part is non-atomic, fix it with conditional UPDATE or INSERT-first locking and re-run.

Gate 3 — Automation engine is enterprise-safe, idempotent, and multi-instance correct. Verify distributed locks protect all background jobs (show the list of jobs and their lock keys), prove that only one instance runs each job at a time, and demonstrate stale lock recovery behaves correctly. Verify idempotency expansion: deterministic trigger event IDs (stable SHA-256 hash with sorted keys) and a database-enforced uniqueness boundary that prevents duplicate actions if the same trigger fires multiple times or multiple workers race. Add explicit tests for duplicate trigger firing and show exactly one action is created. Confirm action creation, queueing, approval, and execution are each auditable and cannot silently “pretend” execution.

Gate 4 — Payments and billing are reliable and honest. Verify Stripe webhook deduplication is durable (DB-backed), race-safe, and replay-safe. Create tests that simulate duplicate webhook deliveries and concurrent deliveries and prove only one user/subscription update occurs. Verify there are no “phantom” payment states: the UI/API must reflect only what exists in Stripe schema or persisted tables. Ensure there are customer input paths for payment methods (subscriptions and any purchasing flows) and verify validation errors are clear and non-destructive. If any payment-related flow can claim success without a corresponding DB write or Stripe confirmation, fix it and add tests.

Gate 5 — Integrations are coherent, observable, and honest. For integration connections, verify credential validation, health checks, error logging (dead letter), retry rules, idempotency keys, and canonical entity mapping behaviors. Ensure every integration event has provenance (source, external IDs, timestamps), and ensure the UI never presents “connected/healthy” unless health checks and credential validation actually passed. Add tests to confirm that inbound/outbound record counts match events logged and that failures enter dead-letter with actionable error categories.

Gate 6 — Data honesty and economic thesis integrity are preserved. Verify the canonical FDR thresholds are the single source of truth and that raw vs confirmed regime are distinct and consistently used: operational decisions must use confirmedRegime, and the UI must label raw vs confirmed honestly. Confirm hysteresis, minimum duration, confirmation readings, and audit trail are functioning and persisted. Add tests that feed boundary FDR values and confirm classification matches thresholds and persistence rules. Ensure any “savings/impact” metrics are separated into estimated vs realized and never displayed as realized unless backed by measured outcomes.

Gate 7 — Operational readiness and observability. Add / verify GET /healthz, GET /readyz, and GET /livez. Ensure structured logging includes correlation IDs for request → action → integration event. Add retention policies for high-volume logs (structured_event_log) and verify the cleanup job works under lock. Add rate limiting for automation mutation and execution endpoints. Ensure crash recovery does not lose critical automation state (pending actions, spend totals, dedup keys).

After building the harness and running it, update or create ENTERPRISE_E2E_CERTIFICATION.md with: an executive summary, scope, environment assumptions (single instance vs multi-instance), full pass/fail table, evidence excerpts, and explicit “safe for beta” recommendations. If anything fails, implement the fix and re-run until all gates pass. Do not invent outputs — only report what the tests actually observed. Use concise, enterprise-grade language.