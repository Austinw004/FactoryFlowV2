I want you to harden and complete my agentic AI automation system so it is enterprise-ready, restart-safe, concurrency-safe, idempotent, auditable, and honest. You must treat the economic thesis logic (FDR regimes + persistence) as the fixed core ground truth and ensure the automation engine consumes it deterministically; the AI must not fabricate outcomes, savings, or performance metrics. My goal is to replace any remaining in-memory runtime Maps and fragile orchestration with fully durable database-backed execution, with strict constraints and safety guarantees.

Start by performing a schema coherence audit and fix any type inconsistencies. In particular, enforce a single canonical type for companyId across ALL tables that reference companies.id. Right now some tables use varchar companyId and others use integer companyId. This must be corrected so referential integrity, joins, and tenant isolation are impossible to break. Generate the exact schema changes and migrations needed, and update all related code paths accordingly. Do not allow any table to remain on a mismatched companyId type.

Next, refactor the runtime automation engine so that rule triggering, guardrail evaluation, action creation, approval workflow, queueing, and execution tracking operate end-to-end against the database tables (aiAgents, aiAutomationRules, aiGuardrails, aiActions, aiApprovalWorkflows, aiExecutionQueue, automationRuntimeState, processedTriggerEvents, structuredEventLog). There must be no dependence on process memory for pending actions, daily spending totals, guardrail violation counts, action history, trigger deduplication, or execution state. The system must survive a restart with zero loss of pending approvals, queued actions, or guardrail state. If any part of the code still uses in-memory Maps for automation runtime state, remove that pattern and migrate fully to durable DB-backed behavior.

Implement strict idempotency for automation triggers using processedTriggerEvents as an insert-first lock, exactly like enterprise webhook handling. The correct pattern must be: compute a deterministic triggerEventId (based on ruleId + triggerType + canonical subject + time bucket if needed) → attempt INSERT into processedTriggerEvents under the unique constraint (companyId, triggerType, triggerEventId) with result status = 'processing' → if unique constraint fails, return immediately without creating or enqueueing actions → if insert succeeds, proceed to action creation and queueing → on success mark the processedTriggerEvents row as processed with actionId and result='processed' → on failure mark as result='failed' with error. There must be no SELECT-before-INSERT and no “process then mark processed.” Business logic must never execute unless the idempotency lock is acquired first.

Implement strict concurrency control for aiExecutionQueue. Add a safe queue claiming mechanism so only one worker can process a queued item at a time. Use an atomic claim operation (e.g., UPDATE status from 'queued' to 'processing' with a WHERE clause, returning the row). Only the process that successfully claims the row may execute it. After execution, update status to completed/failed with timestamps and error details. Ensure retry behavior respects maxAttempts, increments attempts only on actual processing, and writes failures into structuredEventLog and deadLetterEvents where appropriate. Make queue processing safe if multiple intervals or worker instances exist.

Make automationSafeMode a real hard gate. When safeModeEnabled=1 for a company, all actions must default to suggest/auto_draft and never auto_execute unless readinessChecklistPassed=1. If a rule is configured as auto_execute while safe mode is on, it must be forced into approval-required mode and logged as a policy override in structuredEventLog. Add an explicit readiness checklist validator that confirms: integrations are configured, payment method/terms are set if procurement execution is enabled, approval workflows exist for high-value actions, guardrails are present and enabled, and FDR regime feed is healthy (confirmedRegime available). Only then can readinessChecklistPassed be set.

Harden guardrails so they are enforced transactionally and auditable. Spending limits and daily counts must use automationRuntimeState (unique per company per day) and be updated atomically when actions are executed (or drafted if you choose), so the guardrail cannot be bypassed by concurrency or restart. Guardrail violations must increment violationCount and set lastViolationAt in aiGuardrails, and must be written to structuredEventLog with category='guardrail' and success=0. All guardrail decisions must be reproducible from stored inputs.

Harden the approval workflow. If requiresApproval=1, the system must create aiActions with status='awaiting_approval', set approvalDeadline, and not execute until approval is granted. Implement multi-step aiApprovalWorkflows enforcement (steps JSON). Approval escalation rules must be respected. Approval actions must be audited (approvedBy, approvedAt, rejectedBy, rejectedAt, rejectionReason). Expired approvals must not execute and must be marked cancelled/expired with logs.

Ensure honest metrics. executionCount, successRate, avgSavings, and aiPerformanceMetrics must only be derived from real executed actions and real measured outcomes. If you cannot measure savings yet, store null or 0 and label it clearly; do not fabricate. Any “avgSavings” or “totalSavingsReported” must be computed only from actualImpact records after execution, never estimatedImpact. If estimatedImpact is shown, it must be clearly labeled as estimated.

Integrate the behavioral moat system safely. The system must record signal exposures and user actions factually without nudging or optimizing outputs. Enforce the rule: model logic is fixed; behavior is observed; no manipulation. Ensure anonymizedOrgId is generated deterministically but non-reversible from companyId (salted hash with a server secret), and ensure access controls prevent company users from seeing cross-tenant aggregates. Aggregated behavioralPatternAggregates and platform analytics must be owner/admin-only and never expose identifiable data. Implement opt-out controls per company for behavioral collection and ensure the UI can represent that honestly.

Unify integration state to prevent drift. Define one integration “source of truth” table for connection status (choose integrationConnections as primary unless you have a better reason). integrationConfigs may store credentials/config; integrationConnections should represent connection state and verified status; integrationEvents is the event stream. Ensure statuses do not conflict: if a connection is disabled or failed, the system must not present it as connected elsewhere. Ensure all integration events write to integrationAuditLogs and structuredEventLog, and failed events go to deadLetterEvents with clear resolution paths.

Finally, ensure procurement execution is enterprise-safe with payments/terms prerequisites. For any automation rule actionType that creates purchase orders, RFQs, or supplier-facing commitments, validate that the company has configured at least one valid payment method OR explicit credit terms OR a “manual settlement required” mode, and that supplier linkages exist. If payment/terms are missing, the system must not auto-execute; it must auto-draft and request approval while logging the missing prerequisite. Implement these prerequisite checks as first-class validations before execution.

After implementing, produce: (1) the exact code changes needed (routes/services) to migrate runtime execution fully to DB; (2) any schema/migration changes; (3) a deterministic end-to-end stress test harness that simulates trigger storms, retries, restarts, concurrency, and approval delays and proves the system does not duplicate actions, does not exceed spend limits, does not lose queued work, and never fabricates metrics; (4) a clear list of any remaining limitations that prevent true enterprise scaling. The definition of “done” is: no in-memory automation state, atomic idempotency for triggers and queue execution, safe mode enforced, honest metrics only, integration states consistent, behavioral moat recorded ethically and internally, and procurement automation cannot execute without payment/terms prerequisites and approvals.