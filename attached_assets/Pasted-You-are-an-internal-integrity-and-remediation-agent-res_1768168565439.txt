You are an internal integrity and remediation agent responsible for restoring structural coherence to the FDR regime classification system before any further predictive testing occurs. The system is currently incoherent because multiple conflicting threshold definitions exist across the codebase, including at least one inverted logic definition that has contaminated stored regime labels and caused daily flickering that contradicts the theory of slow-moving macro regimes. Your mandate is to restore a single, theory-faithful regime definition, repair historical classifications, enforce persistence and hysteresis so regimes behave like regimes, and implement permanent guardrails that prevent duplicate or inverted thresholds from ever influencing the application again.

Establish a single source of truth for all regime thresholds and mappings. The canonical threshold definitions in economics.ts must be the only authoritative implementation used for classification anywhere in the application, including UI displays, database writes, API responses, stress tests, and evaluation harnesses. Any other file that defines thresholds, hard-codes ranges, or maps FDR values to regimes must be refactored to import the canonical definitions from economics.ts or be isolated so it cannot affect runtime behavior. Any inverted or conflicting definition, including the one identified in fdrStressTest.ts, must be treated as a contamination source: it may remain only as a deliberately isolated test artifact, and it must be impossible for it to write to the database, influence user-facing output, or override the canonical thresholds in any execution path.

Repair data integrity by treating stored or externally provided regime labels as untrusted. Whenever a record contains a regime label, you must recompute the canonical regime directly from the observed FDR using economics.ts and compare the two. If the stored or external regime does not match the canonical classification, the canonical classification must override it for all internal logic and all user-facing output, and the mismatch must be logged as a critical integrity violation with enough detail to diagnose the source (record identifier, timestamp, observed FDR, stored regime, canonical regime, and which code path supplied the stored label). The system must never accept a regime label that contradicts the canonical FDR range, and it must never persist a label that is not derived from economics.ts.

Reclassify historical data retroactively so the database becomes coherent. Identify all historical records that contain a regime label and recompute the regime for each record using the canonical economics.ts thresholds and the record’s observed FDR. Replace the legacy label with the canonical label and preserve an auditable change log that includes the previous label, the corrected label, the observed FDR, the canonical threshold range that applied, and the reason for correction (“legacy thresholds mismatch” or “inverted logic contamination”). This reclassification must be repeatable and idempotent, meaning it can be rerun safely without compounding changes or creating inconsistencies.

Enforce regime behavior consistent with the theory by preventing flicker. Regimes are not point estimates and must not change in response to short-term noise. Introduce a persistence filter such that a regime transition cannot be confirmed until the FDR remains on the new side of the relevant threshold for a minimum confirmation window, and introduce hysteresis bands around each threshold so boundary churn does not cause repeated oscillation. Until a transition is confirmed, the system must keep the current confirmed regime as the official state and must not write a new regime label into persistent storage. If you need an intermediate state for internal monitoring, it must be explicitly provisional and must not be treated as a regime change. Default persistence and hysteresis settings should be conservative and consistent with regime durations measured in months, not days, while remaining configurable for experimentation and testing.

Reframe predictive testing so it becomes meaningful only after coherence is restored. Do not run validity tests on mixed-label or incoherent historical data. After reclassification and persistence enforcement are complete, rerun the integrity audit to confirm that regime labels match canonical thresholds, that regime durations are plausible, and that transitions are rare and interpretable. Only then should predictive testing proceed, and it must explicitly test whether regime transitions align with meaningful macro and operational changes rather than whether the model “predicts” its own autocorrelation.

Install permanent guardrails to prevent recurrence. Add automated checks that fail builds or deployments if any threshold definitions exist outside economics.ts or if any file attempts to map FDR values to regimes without importing canonical definitions. Add runtime assertions that detect impossible classifications, boundary violations, and inverted mappings, and block any database writes when such violations are detected. Maintain a single, visible internal report that confirms which threshold set is active, when it was last updated, and whether any integrity violations have occurred. The system must treat threshold duplication, inverted logic, and unvalidated external labels as critical defects, not acceptable variance.

The governing principle is that a regime framework must first behave like a regime framework before it can be judged on predictive performance. Structural coherence, a single source of truth, validated classification, retroactive repair, and persistence constraints are prerequisites. You are explicitly prohibited from “improving” predictive results by changing the theory or smoothing outcomes with narrative. The only acceptable improvements at this stage are those that make the implementation faithful, auditable, and stable so that future validation is real rather than accidental.
