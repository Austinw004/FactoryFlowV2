Update the existing enterprise-e2e certification harness to eliminate “paper proofs,” remove inconsistencies, and produce auditor-grade evidence. Treat the current report as a draft and improve the harness so every “PASS” is backed by either (a) a runtime assertion against a real endpoint, (b) a database assertion that confirms the intended invariant, or (c) a deterministic static proof that is unavoidable (e.g., a required TypeScript type constraint plus a failing build if violated), and ensure every evidence block is internally consistent. Start by normalizing all spend-limit concurrency tests to one canonical scenario (50 concurrent requests × $5 against $100 limit) and update Gate 2 to run this exact case and print allowed/blocked counts and final spend; do not change the case per run. Next, fix Gate 3’s lock coverage scanner: enumerate the background job declarations, extract the actual job names, and assert each scheduled job is wrapped in withJobLock (or equivalent), then print the job list and pass/fail per job; if the scanner cannot find jobs, fail the gate rather than printing a misleading “0 of 15.” Then convert the highest-risk “source inspection” tests into runtime proofs: for Gate 1, execute HTTP-level negative tests for cross-tenant GET/PATCH/DELETE on at least three entity types in addition to storage tests, and include response codes plus post-conditions; for Gate 5, actually call the integration health endpoint, force at least one synthetic failure that lands in dead-letter, and assert an integrationEvents row exists with an idempotencyKey; for Gate 7, verify rate limiting by making N rapid requests and asserting 429 behavior, and verify structured logging persistence by writing a warn+ event and reading it back from structured_event_log. Require that the harness writes two artifacts on every run: a human report (ENTERPRISE_E2E_CERTIFICATION.md) and a machine JSON artifact with all raw evidence (requests made, status codes, DB rows counts, IDs), and require that any test that relies purely on code scanning explains why runtime proof is not possible and references the exact file/line patterns matched. Finally, add a “Consistency Linter” step at the end of the harness that checks for contradictions in the report itself (e.g., any evidence statement that says ‘0 found’ while the test claims PASS, mismatched numeric scenarios across gates, missing durations, missing inputs), and fail the run if any contradictions exist. When finished, re-run the harness and regenerate the report so it is consistent, scenario-stable, runtime-backed where it matters, and safe to share with enterprise security teams.