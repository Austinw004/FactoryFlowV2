I want you to perform a full enterprise-grade hardening and refactor of my Stripe webhook system and subscription lifecycle logic. This must not simplify functionality or remove any existing business behavior, but it must eliminate all race conditions, deduplication weaknesses, and unsafe state transitions. The result must be safe under concurrent webhook retries, horizontal scaling, and retry storms from Stripe. Pull no punches.

First, refactor webhook idempotency to implement a strict insert-first atomic locking model using the stripeProcessedEvents table as the single source of truth. The correct execution order must be: verify Stripe signature using Stripe’s constructEvent (or verified equivalent from stripe-replit-sync) → extract event ID from the verified object → attempt to insert a new row into stripeProcessedEvents with status = 'processing' → if insertion fails due to unique constraint (23505), immediately return without executing any business logic → if insertion succeeds, proceed with processing → on successful completion, update status to 'processed' and set processedAt timestamp → on failure, update status to 'failed' and persist the error message and stack trace. There must be no SELECT-before-INSERT pattern. The INSERT must function as the concurrency lock. Business logic must never execute without first acquiring this lock. All idempotency must be database-backed only. No in-memory deduplication is allowed.

Ensure the stripeProcessedEvents schema supports: eventId (unique), eventType, customerId, subscriptionId, status ('processing' | 'processed' | 'failed'), createdAt, processedAt, and errorMessage (nullable). If adjustments are required, generate migration SQL.

Second, implement a deterministic subscription state transition guard layer. Subscription status in the users table must follow a monotonic, validated transition model. Enforce rules such as: active cannot regress to incomplete without invoice.payment_failed; canceled can only be set by customer.subscription.deleted; past_due can only be set by invoice.payment_failed; active can only be restored by invoice.paid; trial logic must not downgrade active subscriptions. Before executing any UPDATE users query, validate the current state against the incoming transition. If the transition is invalid, log a structured warning and ignore it. Do not allow silent downgrades or inconsistent states.

Third, ensure the entire payment lifecycle is audit-ready. All webhook failures must persist in stripeProcessedEvents with status='failed' and full error details. Refunds must log chargeId, customerId, amountRefunded, totalAmount, and whether it is a full refund. Subscription updates must log userId, customerId, subscriptionId, status, and tier. All logging must be structured and consistent. There must be no scenario where a webhook retry causes duplicate subscription activation, duplicate cancellation, or ambiguous subscription state.

Fourth, ensure database writes are atomic. Use transactions where necessary so that user updates and event status updates remain consistent. If subscription processing fails mid-execution, the event must remain marked as failed, and no partial state mutation may leave the user record inconsistent.

After implementing all changes, provide: full updated WebhookHandlers.processWebhook implementation, any modified subscription handlers, updated schema definitions, required migration SQL, and a brief explanation of why the system is now safe under retry storms, concurrency, and horizontal scaling. The end result must meet enterprise production standards suitable for technical diligence review and payment compliance scrutiny.

Do not remove functionality. Do not reduce scope. Harden only.