I want you to perform a full enterprise hardening audit of my automation, integration, payment, behavioral observation, and financial execution systems. Do not add new features, do not redesign the product, and do not suggest architectural changes unless they are strictly required to prevent data corruption, financial risk, race conditions, duplication, tenant leakage, or misreporting. This audit must assume the system will operate under high concurrency, multi-instance deployment, restart scenarios, and partial network failures. Your objective is to eliminate correctness risk, transactional ambiguity, and operational dishonesty. You must think like an enterprise liability auditor, not a feature engineer.

Begin by analyzing the entire automation lifecycle from trigger detection through rule evaluation, guardrail enforcement, ai_actions creation, approval workflow routing, execution queue insertion, execution processing, and post-execution metric updates. Identify every point where race conditions could allow duplicate execution, double-triggering, overspending beyond guardrails, bypassing approval requirements, or conflicting status transitions. Refactor where necessary so that trigger processing is idempotent using processedTriggerEvents with atomic insert enforcement, guardrail evaluation and runtime spend updates occur inside a single database transaction, queue claim operations use atomic status transitions (for example, queued to processing with a conditional update), and action execution is idempotent using actionId-based keys. Ensure that no financial or operational correctness depends on in-memory state and that every mutation affecting money, procurement, inventory, or supplier state is durable and transactionally safe.

Next, audit tenant isolation. You must verify that every query touching company-scoped data includes explicit companyId filtering and that no storage-layer method can operate without tenant context. Standardize companyId types if inconsistencies exist across tables. Confirm that no integration, automation, webhook, approval, or background job can operate without tenant scoping. If any query or mutation path bypasses tenant filtering, flag and correct it.

Then validate all payment and procurement flows. Any action that claims to create a purchase order, generate an RFQ, execute a payment, or report cost savings must validate supplier linkage, confirm payment method configuration, support invoice-based enterprise payment flows in addition to card-based flows, and clearly distinguish between drafted, approved, executed, settled, and reconciled states. No savings, revenue, or impact metrics may be recorded unless tied to a resolved financial record. Ensure webhook deduplication is fully durable and that no subscription or invoice event can be processed twice under restart or concurrency.

After that, enforce outcome honesty. Confirm that successRate, avgSavings, totalCostSavings, and all AI performance metrics derive strictly from resolved predictionOutcomes or completed financial records. Ensure estimatedImpact and actualImpact are structurally separated and that actualImpact is written only after execution resolution. Verify that no UI surface can display unverified impact claims or inflate performance metrics. Ensure prediction falsification discipline is intact and that prediction outcomes correctly capture regimeAtPrediction, regimeAtOutcome, and errorMagnitude without allowing retroactive manipulation.

Next, harden integration reliability. Confirm that integrationEvents enforce idempotency via idempotency keys and reject duplicates. Ensure deadLetterEvents are safely replayable without duplicate canonical entity creation. Validate that syncJobs use checkpointing and cannot double-import or double-export records. Confirm canonicalEntities and canonicalEntityMappings prevent duplicate resolution and that conflict handling increments conflictCount deterministically. Integration health snapshots must reflect real latency and error rates rather than static status flags.

Then validate that automationSafeMode is operationally real. When safeModeEnabled is true, all auto_execute actions must automatically downgrade to suggest or auto_draft. Guardrail violations must escalate instead of silently blocking. High-value or high-risk actions must require approval regardless of rule-level autonomy. Confirm that safe mode cannot be bypassed through direct queue insertion or API calls.

Afterward, enforce concurrency resilience. Implement atomic claim logic for aiExecutionQueue using conditional updates. Enforce maxAttempts with exponential backoff. Automatically requeue actions stuck in processing beyond a defined threshold. Ensure crash recovery cannot result in duplicate execution. Confirm that restart, replay, or overlapping background jobs cannot corrupt runtime totals or financial counters.

Then verify observability and audit integrity. All automation executions, guardrail violations, approval decisions, webhook receipts, integration failures, and financial state changes must log to structuredEventLog with companyId context. Logs must include success flags, durationMs, and errorMessage where relevant. Audit trails must be sufficient to reconstruct any financial or operational decision.

Finally, validate the behavioral observation system. Confirm that observational tables cannot alter model logic. Ensure that every behavioral insight in behavioralAuditTrail references concrete exposure and action IDs. Validate that aggregated behavioral patterns cannot expose identifiable organization data. Confirm that predictionOutcomes correctly link to regime snapshots and that no feedback loop contaminates the fixed economic model logic.

At the conclusion of this audit, provide a structured output that includes a list of enterprise-blocking risks, a list of required structural corrections, a list of optional improvements, and a clear determination of whether the system can survive restart, replay, duplicate events, and concurrent execution without financial, operational, or data corruption risk. Do not summarize. Identify weaknesses precisely and propose direct corrections where necessary.